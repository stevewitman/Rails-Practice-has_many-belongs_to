<div class="page-header">
  <h1><%= @cat.name %></h1>
</div>

<h3>This cat's toys are...</h3>

<div class="well">
  <% @cat.toys.each do |toy| %>
  <ul>
    <li>
      <%= link_to toy.name, toy_path(toy) %>
    </li>
  </ul>
  <% end %>
</div>
